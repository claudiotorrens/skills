{
  "name": "spacetime-memory",
  "acceptLicenseTerms": true,
  "version": "1.0.2",
  "description": "High-performance SpacetimeDB memory integration for OpenClaw.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "openclaw config set skills.entries.spacetime-memory.enabled true && openclaw gateway restart || echo 'Restart gateway and run /new to load tools.'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "spacetimedb": "^2.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
